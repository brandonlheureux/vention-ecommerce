import styled from '@emotion/styled';
import { Container, Box } from '@mui/material';

export const ExpandContent = styled(Container)`
    flex-grow: 1;
`;

export const Screen = styled(Box)`
  display: flex;
  flex-direction: column;
  height: 100%;
  width: 100%;
`;
